import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Orbil_ion_1.1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
